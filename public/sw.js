// A basic service worker for PWA installation
self.addEventListener('fetch', (event) => {
  // This service worker is primarily for making the app installable.
  // You can add more advanced caching strategies here later.
});
